/* form */
const form = document.querySelector('form');

/* Inputs */
const type = document.querySelector('#type');
const brand = document.querySelector('#brand');
const price = document.querySelector('#price');
const number = document.querySelector('#number');

/* Display */
const resultat = document.querySelector('#resultat');

const ultype = document.querySelector('#check_type');
const ulbrand = document.querySelector('#check_brand');
const ulprice = document.querySelector('#check_price');
const ulnumber = document.querySelector('#check_number');

/* Event listeners */
form.addEventListener('submit', (e) => {
    e.preventDefault();

});

function checkType(typeValue) {
    // Check if typeValue is valid
}

function checkBrand(brandValue) {
    // Check if brandValue is valid
}

function checkPrice(priceValue) {
    // Check if priceValue is valid
}

function checkNumber(numberValue) {
    // Check if numberValue is valid
}
